# yaml-language-server: $schema=./schema.yml

- category: null
  snippets:
    - name:
        - dom
        - domain
      body: \operatorname{dom}

- category: category
  snippets:
    - name:
      - cod
      - codomain
      body: \operatorname{dom}
